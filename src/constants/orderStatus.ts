enum OrderStatus {
  NEW = 'new',
  ACCEPTED = 'accepted',
  ARRIVED = 'arrived',
  RATING = 'rating',
  WAITING = 'pause',
  PROCESSING = 'processing',
  DONE = 'done',
  CANCELED = 'canceled',
}

export default OrderStatus;
